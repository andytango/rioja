---
import { getCollection } from "astro:content";
import Layout from "../../components/Layout.astro";
import BackLink from "../../components/BackLink.astro";

export async function getStaticPaths() {
    const posts = await getCollection("blog", ({ data }) => {
        // Drafts render in dev so they can be previewed, but never in a prod build.
        return import.meta.env.PROD ? data.draft !== true : true;
    });

    return posts.map((post) => ({
        params: { slug: post.slug },
        props: post,
    }));
}

const post = Astro.props;
const { Content } = await post.render();
---

<Layout
    title={`${post.data.title} - rioja`}
    description={post.data.description}
    type="article"
>
    <main class="max-w-6xl mx-auto px-4 py-12">
        <!-- Article Header -->
        <article>
            <header class="max-w-[42rem] mx-auto mb-12 pb-8 border-b border-amber-950/10 dark:border-orange-100/10">
                <!-- Navigation -->
                <nav class="mb-12">
                    <BackLink href="/blog" text="Blog" />
                </nav>
                <h1 class="text-3xl sm:text-4xl lg:text-5xl font-mono font-medium mb-6">
                    {post.data.title}
                </h1>
                <div class="font-mono text-sm text-amber-600 dark:text-amber-400">
                    <time datetime={post.data.pubDate.toISOString()}>
                        {
                            post.data.pubDate.toLocaleDateString("en-US", {
                                year: "numeric",
                                month: "short",
                                day: "numeric",
                            })
                        }
                    </time>
                </div>
            </header>

            <!-- Article Content -->
            <div
                class="blog-content prose prose-lg max-w-none
          prose-headings:font-mono prose-headings:text-amber-950 dark:prose-headings:text-orange-100 prose-headings:font-medium
          prose-h2:text-2xl prose-h2:mt-16 prose-h2:mb-6
          prose-h3:text-lg prose-h3:mt-10 prose-h3:mb-4 prose-h3:border-l-2 prose-h3:border-amber-600/40 dark:prose-h3:border-amber-400/40 prose-h3:pl-3
          prose-p:text-amber-950/90 dark:prose-p:text-orange-100/90 prose-p:leading-relaxed prose-p:mb-6
          prose-a:text-amber-600 dark:prose-a:text-amber-400 prose-a:underline hover:prose-a:text-amber-950 dark:hover:prose-a:text-amber-300
          prose-strong:text-amber-950 dark:prose-strong:text-orange-100 prose-strong:font-semibold
          prose-ul:list-disc prose-ul:ml-6 prose-ul:mb-6
          prose-ol:list-decimal prose-ol:ml-6 prose-ol:mb-6
          prose-li:text-amber-950/90 dark:prose-li:text-orange-100/90 prose-li:mb-2
          prose-code:font-mono prose-code:text-amber-600 dark:prose-code:text-amber-400 prose-code:text-sm
          prose-pre:bg-amber-950 dark:prose-pre:bg-stone-800 prose-pre:text-amber-50 prose-pre:font-mono prose-pre:mb-6
          prose-blockquote:border-l-2 prose-blockquote:border-amber-600 dark:prose-blockquote:border-amber-400 prose-blockquote:pl-4 prose-blockquote:italic prose-blockquote:text-amber-950/80 dark:prose-blockquote:text-orange-100/80 prose-blockquote:mb-6
          prose-th:text-amber-950 dark:prose-th:text-orange-100 prose-td:text-amber-950/90 dark:prose-td:text-orange-100/90 prose-td:border-amber-950/10 dark:prose-td:border-orange-100/15"
            >
                <Content />
            </div>

            <!-- Updated Date -->
            {
                post.data.updatedDate && (
                    <footer class="max-w-[42rem] mx-auto mt-12 pt-6 border-t border-amber-950/10 dark:border-orange-100/10">
                        <p class="text-sm font-mono text-amber-600 dark:text-amber-400">
                            Updated:{" "}
                            {post.data.updatedDate.toLocaleDateString("en-US", {
                                year: "numeric",
                                month: "short",
                                day: "numeric",
                            })}
                        </p>
                    </footer>
                )
            }
        </article>

        <!-- Back to Blog -->
        <div class="max-w-[42rem] mx-auto mt-16 pt-8 border-t border-amber-950/10 dark:border-orange-100/10">
            <BackLink href="/blog" text="Blog" />
        </div>
    </main>
</Layout>

